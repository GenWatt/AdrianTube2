@using AdrianTube2.Models.Movie

<ul class="d-flex gap-3 flex-wrap">
    @foreach (var movie in Movies)
    {
        <MovieCard Movie="movie" />
    }
</ul>


@code {
    [Parameter] public IReadOnlyList<Movie> Movies { get; set; }
}